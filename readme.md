# Base Application

### Install

* Run: `composer update --no-scripts`
* Run: `php artisan migrate`
* Run: `npm install`
* Run: `bower install`
* Run: `gulp` & `gulp watch` to watch for file changes

